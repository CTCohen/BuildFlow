{
  "name": "buildflow-website",
  "version": "0.1.0",
  "description": "Fornax landing page",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "astro": "^4.0.0"
  }
}
