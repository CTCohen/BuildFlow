# Copy to .env for local dev, or set in Railway per client.
CLIENT=demo-plumbing
SITE_URL=https://northgateplumbing.com
# Lead handler. Without it, the quote form falls back to mailto: the owner.
FORM_ENDPOINT=
