---
title: Brand (BuildFlow's own)
purpose: BuildFlow's own logo, brand identity, and voice — NOT the customer-facing design system. See docs/CUSTOMER_VS_INTERNAL.md for the full split.
status: draft
owner: c.t.cohen
updated: '2026-09-22'
tier_scope: all
phase: phase_1
---

# Brand

This folder is BuildFlow's own identity — the company, not any customer's site.

**If you're looking for the customer website design system (templates, style profiles, tokens), that's in
`design/` and `app/src/tokens/`, not here.** See `docs/CUSTOMER_VS_INTERNAL.md` for the full customer-vs-internal
split before adding anything to either folder.

## What goes here (once built)
- BuildFlow's logo files and usage rules
- Brand color and type system for BuildFlow's own marketing (separate from the customer style-profile system)
- Voice/tone guide for BuildFlow's own copy — distinct from `messaging/`, which is customer-facing outreach

## Status
Empty. This is a `[decision: Tyler picks a direction]` item (see `operations/TYLER_QUEUE.md`) — nothing gets
built here until he does. `website/` (BuildFlow's own marketing site) will draw on whatever lands here once it
exists; until then it uses placeholder styling.
